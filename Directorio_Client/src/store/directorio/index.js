import * as actions from "./actions";
import * as gatters from "./gatters";
import * as mutations from "./mutations";
import * as state from "./state";

const directorioStore = {
  actions,
  gatters,
  mutations,
  state,
};

export default directorioStore;
